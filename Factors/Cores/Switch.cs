namespace Factors.Cores
{
    //- We could make a class that consolidates multiple Reactor cores, by putting all of the triggers in
    //  a Dictionary<IFactor, int> that marked what 'trigger group' each belonged to.
    //- What if a trigger belongs to multiple groups?
    // public class Switch : ReactorCore
    // {
    //     
    // }
}